# forge generates learned data OUTSIDE this repo (~/.claude/forge-data/).
# These patterns are a defensive belt — never commit learned data, logs, or secrets,
# which can contain raw commands, file paths, internal details, or tokens.
forge-data/
*.jsonl
*.log
LESSONS.md

# Publishing guidance for Claude — local only, not part of the published plugin.
CLAUDE.md

# Node / OS noise
node_modules/
.DS_Store
Thumbs.db
*.local
.env
.env.*
